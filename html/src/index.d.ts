export declare global {
  interface Window {
    handle: any;
    utf16le: any;
  }
}

export declare global {
  interface Document {
    handle: any;
  }
}

export declare global {
  interface globalThis {
    handle: any;
  }
}
